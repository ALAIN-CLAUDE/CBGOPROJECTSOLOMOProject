({
	fetchFundingRequest : function(component, event, helper) {
		helper.fetchFundingRequest(component, event, helper);
        //helper.testUserId(component, event, helper);
	},
    
    
    openFundingRequest : function(component, event, helper) {
		helper.openFundingRequest(component, event, helper);
	},
    
    closeModal : function(component, event, helper) {
		helper.closeModal(component, event, helper);
	},
    
})